const TimelineLine = () => {
  return (
    <div
      style={{
        width: "2px",
        height: "100%",
        backgroundColor: "var(--timeline-line)",
        borderRadius: "50%",
      }}
    />
  );
};

export default TimelineLine;
